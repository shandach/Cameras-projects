# 🏢 Workplace Monitoring System

On-Prem система мониторинга присутствия сотрудников на рабочих местах банка с использованием видеоаналитики.

## 📋 Возможности

- 🎥 **Multi-camera RTSP** — поддержка нескольких IP-камер
- 🔍 **YOLOv8 детекция** — распознавание людей в реальном времени
- ✏️ **Интерактивный ROI редактор** — создание зон мышью
- ⏱️ **Логика времени** — 3 сек вход / 10 сек выход
- 💾 **SQLite БД** — хранение сессий локально
- 🔐 **Безопасность** — видео НЕ сохраняется и НЕ передаётся

## 🚀 Установка

```bash
pip3 install -r requirements.txt
```

## ⚙️ Настройка камер

1. Скопируйте файл конфигурации:
```bash
cp .env.example .env
```

2. Отредактируйте `.env` с вашими RTSP адресами:
```env
CAMERA_1_URL=rtsp://192.168.1.100:554/stream
CAMERA_1_NAME=Филиал 1 - Место 1

CAMERA_2_URL=rtsp://192.168.1.101:554/stream
CAMERA_2_NAME=Филиал 1 - Место 2
```

3. При необходимости добавьте аутентификацию:
```env
RTSP_USER=admin
RTSP_PASSWORD=your_password
```

## ▶️ Запуск

```bash
python3 main.py
```

## 🎮 Управление

| Клавиша | Действие |
|---------|----------|
| **R** | Начать рисовать зону ROI |
| **Enter** | Сохранить зону |
| **D** | Удалить последнюю зону |
| **C** | Очистить все зоны камеры |
| **N** | Следующая камера |
| **P** | Предыдущая камера |
| **S** | Статистика |
| **H** | Подсказки |
| **Q** | Выход |

## 📊 Логика статусов

```
🟩 VACANT → Человек вошёл → 🟥 OCCUPIED (ждём 3 сек)
          ↓ Остался 3+ сек
          🟥 OCCUPIED + Таймер работает
          ↓ Вышел
          🟥 Пауза (ждём 10 сек)
          ↓ Не вернулся 10+ сек
          🟩 VACANT + Сессия сохранена в БД
```

## 📁 Структура проекта

```
workplace-monitoring/
├── main.py              # Точка входа
├── config.py            # Настройки и загрузка камер
├── .env                 # Конфигурация камер (не в git!)
├── .env.example         # Шаблон конфигурации
│
├── core/
│   ├── detector.py      # YOLOv8 детекция
│   ├── roi_manager.py   # Управление зонами
│   ├── occupancy_engine.py  # Логика времени
│   └── stream_handler.py    # RTSP захват
│
├── database/
│   ├── models.py        # SQLAlchemy модели
│   └── db.py            # Операции с БД
│
└── gui/
    ├── roi_editor.py    # Редактор зон
    └── display.py       # Отрисовка UI
```

## 🔒 Безопасность

- ✅ Видео НЕ сохраняется и НЕ передаётся
- ✅ Только числовые данные хранятся в локальной БД
- ✅ RTSP пароли хранятся в `.env` (игнорируется git)
- ✅ Соответствует требованиям банковской безопасности

## 📈 Масштабирование

Система готова к расширению:
- Добавляйте камеры в `.env` (CAMERA_3, CAMERA_4, ...)
- Для 500+ камер рекомендуется PostgreSQL
